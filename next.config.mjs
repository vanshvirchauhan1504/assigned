/** @type {import('next').NextConfig} */

const nextConfig = {
    images: {
      domains: ['astralpaints.kwebmakerdigitalagency.com'], // Add the domain here
    },
  };
  
export default nextConfig;